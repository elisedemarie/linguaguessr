[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
common = { path = "../common" }
leptos = { version = "0.7", features = ["csr"] }
wasm-bindgen = "0.2"
